🎯 **What:** Added model validation and edge case tests for the `Faturamento` model using standard DataAnnotations validation context to ensure that a fully hydrated object has no validation errors and that the constructor assigns expected defaults like `StatusPagamento` properly.

📊 **Coverage:** Covered default instantiation for checking `StatusPagamento` property default state to `Pendente`, and verified that a complete set of populated properties results in zero validation errors from `TryValidateObject`.

✨ **Result:** Improved test coverage on the domain models, ensuring validation attributes and default field states are functioning and verified.
