💡 **What:** Removed a dead/commented line in `Program.cs` (`// Fix para erro de timezone no PostgreSQL...`).

🎯 **Why:** Removing dead code and unnecessary comments improves code readability and reduces noise, making it easier for developers to understand the active code.

✅ **Verification:** Ran `dotnet build` and `dotnet test AssistenciaTech.sln` to ensure no functionality was broken. All tests passed.

✨ **Result:** A cleaner and more maintainable `Program.cs` file without changing any application behavior.
